# راهنمای اجرا و استفاده - برنامه مدیریت میکروتیک

## 📱 اجرای برنامه روی اندروید

### روش 1: استفاده از Emulator

```bash
# لیست emulator های موجود
flutter emulators

# راه‌اندازی emulator
flutter emulators --launch <emulator-id>

# اجرای برنامه
flutter run
```

### روش 2: دستگاه واقعی

1. USB Debugging را در تنظیمات Developer Options فعال کنید
2. دستگاه را با USB به کامپیوتر وصل کنید
3. دستور زیر را اجرا کنید:

```bash
flutter devices  # چک کردن دستگاه
flutter run      # اجرای برنامه
```

### روش 3: ساخت APK

```bash
# Debug APK (برای تست)
flutter build apk --debug

# Release APK (برای انتشار)
flutter build apk --release

# فایل APK در این مسیر ساخته می‌شود:
# build/app/outputs/flutter-apk/app-debug.apk
# build/app/outputs/flutter-apk/app-release.apk
```

## 🔧 تنظیمات مهم برای اتصال به روتر

### شبکه محلی

**حالت 1: دستگاه واقعی**
- دستگاه و روتر باید در یک شبکه WiFi باشند
- IP روتر را مستقیماً وارد کنید (مثلاً: `192.168.85.1`)

**حالت 2: Emulator**
- برای localhost روی کامپیوتر: از `10.0.2.2` استفاده کنید
- برای دستگاه‌های دیگر در شبکه: IP واقعی را وارد کنید

### پورت‌های RouterOS

- **API (بدون SSL):** پورت `8728` (یا پورت سفارشی شما: `8788`)
- **API-SSL:** پورت `8729` (هنوز پیاده‌سازی نشده)

## 🧪 تست برنامه

### اطلاعات تست شما

```
Host: 192.168.85.1
Port: 8788
Username: hsco
Password: Hs-co@12321#
```

### مراحل تست

1. برنامه را باز کنید
2. اطلاعات فوق را در صفحه Login وارد کنید
3. (اختیاری) "Remember Me" را فعال کنید
4. دکمه "Login" را بزنید
5. در صورت موفقیت، به صفحه Dashboard منتقل می‌شوید

## 🐛 عیب‌یابی

### مشکل 1: اتصال برقرار نمی‌شود

**چک کنید:**
- آیا دستگاه به WiFi متصل است؟
- آیا روتر در دسترس است؟ (ping کنید)
- آیا پورت API فعال است؟
- آیا فایروال روتر اجازه اتصال می‌دهد؟

**راه حل:**
```bash
# چک کردن دسترسی به روتر (از ترمینال کامپیوتر)
ping 192.168.85.1

# چک کردن پورت
telnet 192.168.85.1 8788
```

### مشکل 2: خطای Authentication

**چک کنید:**
- نام کاربری و رمز عبور صحیح است؟
- آیا کاربر دسترسی API دارد؟

**راه حل:**
در RouterOS:
```
/user print
/user set <username> group=full
```

### مشکل 3: Timeout

**چک کنید:**
- شبکه کند است؟
- روتر پاسخ می‌دهد؟

**راه حل:**
- Timeout را در `AppConstants` افزایش دهید
- از شبکه سریع‌تر استفاده کنید

## 📊 لاگ‌ها و Debug

### مشاهده لاگ‌های اندروید

```bash
# همه لاگ‌ها
flutter logs

# فیلتر شده
flutter logs | grep -i "flutter"

# یا از Android Studio استفاده کنید
# Run -> Flutter Run -> Console
```

### فعال کردن Debug Mode

در کد، می‌توانید print statements اضافه کنید:

```dart
print('🔌 Connecting to $host:$port');
print('📥 Received: $data');
```

## 🔐 امنیت

### نکات مهم

1. **رمز عبور**: در حالت production، از SSL استفاده کنید
2. **Cleartext Traffic**: فقط برای development فعال است
3. **Credentials**: در SecureStorage ذخیره می‌شود (امن)
4. **Certificate Pinning**: برای نسخه بعدی پیاده‌سازی می‌شود

### فعال کردن API-SSL در RouterOS

```
/ip service
set api-ssl port=8729
set api-ssl certificate=<your-cert>
```

## 📦 ساخت نسخه Release

### مراحل

1. **تنظیم signing key:**

ایجاد keystore:
```bash
keytool -genkey -v -keystore ~/key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias key
```

2. **فایل `android/key.properties` بسازید:**

```properties
storePassword=<password>
keyPassword=<password>
keyAlias=key
storeFile=<path-to-key.jks>
```

3. **Build release:**

```bash
flutter build apk --release
flutter build appbundle --release  # برای Google Play
```

## 🚀 انتشار در Google Play

### مراحل

1. Build app bundle:
```bash
flutter build appbundle --release
```

2. فایل در: `build/app/outputs/bundle/release/app-release.aab`

3. در Google Play Console آپلود کنید

### اطلاعات مورد نیاز

- نام برنامه: MikroTik Manager
- Package name: com.example.hsmik (باید تغییر کند)
- نسخه: 1.0.0
- Icon: باید اضافه شود
- Screenshots: باید تهیه شود

## 📝 تغییرات آینده

### فاز بعدی (Dashboard)

- نمایش منابع سیستم (CPU, Memory, Disk)
- نمایش اینترفیس‌ها
- نمایش uptime و version

### فیچرهای برنامه‌ریزی شده

- [ ] مدیریت اینترفیس‌ها
- [ ] مدیریت IP Address
- [ ] مدیریت DHCP Server
- [ ] مدیریت Firewall Rules
- [ ] لاگ سیستم
- [ ] پشتیبانی SSL/TLS
- [ ] تم تیره
- [ ] RTL کامل برای فارسی
- [ ] ذخیره چند روتر
- [ ] نمودارهای آماری

## 💡 نکات توسعه

### Hot Reload

بعد از تغییرات، در ترمینال `r` بزنید:
```bash
r  # Hot reload
R  # Hot restart
```

### Debug مستقیم

```bash
flutter run --verbose  # لاگ کامل
flutter run --release  # تست نسخه release
```

### تست روی چند دستگاه

```bash
flutter run -d all  # اجرا روی همه دستگاه‌ها
```

## 📞 پشتیبانی

مشکلی دارید؟
1. لاگ‌ها را چک کنید: `flutter logs`
2. Errors را ببینید: `flutter analyze`
3. Doctor را اجرا کنید: `flutter doctor -v`

---

**موفق باشید! 🚀**
